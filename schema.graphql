"""Exposes a URL that specifies the behaviour of this scalar."""
directive @specifiedBy(
  """The URL that specifies the behaviour of this scalar."""
  url: String!
) on SCALAR

input CreateUserInput {
  name: String
}

type Like {
  count: String!
}

type LikeNewPayloadType {
  like: Like
}

type Mutation {
  """Create a new user"""
  CreateUser(input: CreateUserInput): User
}

type Query {
  version: String
  users: [User]
  user(id: ID!): User
  talks: [Talk]
  talk(id: ID!): Talk
}

type Subscription {
  LikeNew: LikeNewPayloadType
}

type Talk {
  name: String!
  description: String!
  speaker: User!
  host: User!
}

type User {
  name: String!
}

